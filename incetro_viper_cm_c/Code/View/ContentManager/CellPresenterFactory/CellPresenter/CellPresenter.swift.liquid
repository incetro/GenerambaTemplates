{% include 'header' %}

import UIKit

// MARK: - {{ custom_parameters.cell }}CellPresenter

final class {{ custom_parameters.cell }}CellPresenter: GenericCellPresenter<{{ custom_parameters.cell }}Cell> {
    
    // MARK: - Properties
    
    /// ViewModel instance
    private let viewModel: {{ custom_parameters.cell }}CellViewModelProtocol
    
    // MARK: - Initializers

    /// Default initializer
    ///
    /// - Parameter viewModel: ViewModel instance
    init(viewModel: {{ custom_parameters.cell }}CellViewModelProtocol) {
        self.viewModel = viewModel
    }
    
    // MARK: - CellPresenter
    
    override func configureCell(_ cell: {{ custom_parameters.cell }}Cell) {
        cell.setup(with: viewModel)
    }
    
    override func cellSize(reusableCellHolder: UITableView) -> CGSize {
        CGSize(width: reusableCellHolder.frame.width, height: 52)
    }
}
