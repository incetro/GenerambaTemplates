{% include 'header' %}

import UIKit

// MARK: - {{ custom_parameters.cell }}Cell

final class {{ custom_parameters.cell }}Cell: UITableViewCell {
    
    // MARK: - Aliases

    private typealias LayoutConstant = {{ custom_parameters.cell }}CellConstants
    
    // MARK: - Properties
    
    // MARK: - UITableViewCell
    
    required init?(coder aDecoder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }
    
    override init(style: UITableViewCell.CellStyle, reuseIdentifier: String?) {
        super.init(style: style, reuseIdentifier: reuseIdentifier)
        designer.add(designable: self)
    }
    
    override func prepareForReuse() {
        super.prepareForReuse()
    }
    
    // MARK: - Useful
    
    func setup(with viewModel: {{ custom_parameters.cell }}CellViewModelProtocol) {
        
    }
}

// MARK: - Layout

extension {{ custom_parameters.cell }}Cell {

    private func setup() {

    }
}

// MARK: - Actions

extension {{ custom_parameters.cell }}Cell {

}

// MARK: - Designable

extension {{ custom_parameters.cell }}Cell: Designable {

    func design(appearance: Appearance) {
        let design = appearance.{{ custom_parameters.cell | downcase }}CellAppearance
        backgroundColor = design.backgroundColor
    }
}

// MARK: - {{ custom_parameters.cell }}CellConstants

private struct {{ custom_parameters.cell }}CellConstants {
}