{% include 'header' %}

import UIKit

// MARK: - {{ prefix }}{{ module_info.name }}Module

final class {{ prefix }}{{ module_info.name }}Module: Module {

    typealias Input = {{ prefix }}{{ module_info.name }}ModuleInput
    typealias View = {{ prefix }}{{ module_info.name }}ViewController

    required init() {

    }

    static func instantiateTransitionHandler() -> UIViewController {
		return {{ prefix }}{{ module_info.name }}ModuleAssembly().obtainViewController()
    }
}