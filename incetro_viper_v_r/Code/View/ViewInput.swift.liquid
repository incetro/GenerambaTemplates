{% include 'header' %}

// MARK: - {{ prefix }}{{ module_info.name }}ViewInput

protocol {{ prefix }}{{ module_info.name }}ViewInput: class, ViewInput {
    
    /// Setup initial view state
    func setupInitialState()

    /// Update view state with the given view model
    /// - Parameter viewModel: some new view model
    func update(viewModel: {{ prefix }}{{ module_info.name }}ViewModel)
}