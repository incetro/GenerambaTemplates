{% include 'header' %}

import UIKit

// MARK: - {{ prefix }}{{ module_info.name }}View

final class {{ prefix }}{{ module_info.name }}View: UIView {

    // MARK: - Properties
    
    /// Presenter instance
    var output: {{ prefix }}{{ module_info.name }}ViewOutput?

    // MARK: - UIView

    public override func layoutSubviews() {
        super.layoutSubviews()
        DispatchQueue.once(object: self) {
            self.setup()
            self.output?.didTriggerViewReadyEvent()
        }
    }
}

// MARK: - Layout

extension {{ prefix }}{{ module_info.name }}View {

    private func setup() {

    }
}

// MARK: - Actions

extension {{ prefix }}{{ module_info.name }}View {

}

// MARK: - {{ prefix }}{{ module_info.name }}ViewInput

extension {{ prefix }}{{ module_info.name }}View: {{ prefix }}{{ module_info.name }}ViewInput {
    
    func setupInitialState() {

    }

    func update(viewModel: {{ prefix }}{{ module_info.name }}ViewModel) {
    }
}

// MARK: - ViperViewOutputProvider

extension {{ prefix }}{{ module_info.name }}View: ViewOutputProvider {
    
    var viewOutput: ModuleInput? {
        return self.output as? ModuleInput
    }
}

// MARK: - LayoutConstants

private struct LayoutConstants {
}