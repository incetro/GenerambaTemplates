{% include 'header' %}

import UIKit

// MARK: - {{ prefix }}{{ module_info.name }}Module

final class {{ prefix }}{{ module_info.name }}Module {

    // MARK: - Module

    static func instantiate(withTransitionHandler transitionHandler: TransitionHandler) -> {{ prefix }}{{ module_info.name }}View {
        return {{ prefix }}{{ module_info.name }}ModuleAssembly().obtainView(withTransitionHandler: transitionHandler)
    }
}