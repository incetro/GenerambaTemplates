{% include 'header' %}

import Foundation

// MARK: - {{ prefix }}{{ module_info.name }}ViewModelProtocol

protocol {{ prefix }}{{ module_info.name }}ViewModelProtocol {
    
    /// Parent {{ plain_uncap }} object instance
    var {{ plain_uncap }}: {{ plain_uncap }}PlainObject { get }
}
