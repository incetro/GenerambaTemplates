{% include 'header' %}

import Foundation

// MARK: - {{ prefix }}{{ module_info.name }}ViewModel

struct {{ prefix }}{{ module_info.name }}ViewModel: {{ prefix }}{{ module_info.name }}ViewModelProtocol {
    
    // MARK: - Properties

    let {{ plain_uncap }}: {{ custom_parameters.plain }}PlainObject

    // MARK: - Initializers

    /// Default initializer
    /// - Parameters:
    ///   - {{ plain_uncap }}: current {{ custom_parameters.plain }} model
    init({{ plain_uncap }}: {{ plain_uncap }}PlainObject) {
        self.{{ plain_uncap }} = {{ plain_uncap }}
    }
}
