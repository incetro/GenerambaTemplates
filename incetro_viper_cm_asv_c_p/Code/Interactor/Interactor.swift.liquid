{% include 'header' %}

import Foundation

// MARK: - {{ prefix }}{{ module_info.name }}Interactor

final class {{ prefix }}{{ module_info.name }}Interactor {
    
    // MARK: - Properties
    
    /// Presenter instance
    weak var output: {{ prefix }}{{ module_info.name }}InteractorOutput?

    /// {{ prefix }}{{ custom_parameters.service_name }}Service instance
    private let {{ prefix }}{{ custom_parameters.service_name }}Service: {{ prefix }}{{ module_info.service }}Service

    // MARK: - Initializers

    /// Default initializer
    /// - Parameters:
    ///   - {{ custom_parameters.service | downcase }}Service: {{ custom_parameters.service }}Service instance
    init({{ prefix }}{{ custom_parameters.service_name }}Service: {{ prefix }}{{ custom_parameters.service }}Service) {
        self.{{ prefix }}{{ custom_parameters.service_name | downcase }}Service = {{ prefix }}{{ custom_parameters.service_name | downcase }}Service
    }
}

// MARK: - {{ prefix }}{{ module_info.name }}InteractorInput

extension {{ prefix }}{{ module_info.name }}Interactor: {{ prefix }}{{ module_info.name }}InteractorInput {
    
    func obtain{{ custom_parameters.plain }}() -> {{ custom_parameters.plain }}PlainObject {
    	do {
            return try {{ prefix }}{{ module_info.service_name }}Service.obtain().sync().get()
        } catch {
            fatalError(error.localizedDescription)
        }
    }
}