{% include 'header' %}

import Foundation

// MARK: - {{ prefix }}{{ module_info.name }}ViewModelDesigner

protocol {{ prefix }}{{ module_info.name }}ViewModelDesigner {
    
    /// Converts {{ custom_parameters.plain }}PlainObject to {{ prefix }}{{ module_info.name }}ViewModelProtocol
    /// - Parameter {{ custom_parameters.plain_uncap }}: some {{ custom_parameters.plain_uncap }}
    func viewModel(from {{ custom_parameters.plain_uncap }}: {{ custom_parameters.plain }}PlainObject) -> {{ prefix }}{{ module_info.name }}ViewModelProtocol
}
