{% include 'header' %}

import Foundation

// MARK: - {{ prefix }}{{ module_info.name }}ViewOutput

protocol {{ prefix }}{{ module_info.name }}ViewOutput {
    
    /// View is ready
    func didTriggerViewReadyEvent()

    /// Need to reload routes to fit the relevant state
    func didTriggerReload{{ custom_parameters.entities }}Event()

    /// Need to load next page with routes
    func didTriggerLoadNextPageEvent()
}