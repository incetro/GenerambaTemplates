{% include 'header' %}

import Foundation

// MARK: - {{ custom_parameters.cell }}CellViewModel

struct {{ custom_parameters.cell }}CellViewModel: {{ custom_parameters.cell }}CellViewModelProtocol {
    
    // MARK: - Properties

    let {{ custom_parameters.plain | downcase }}: {{ custom_parameters.plain }}PlainObject

    // MARK: - Initializers

    /// Default initializer
    /// - Parameters:
    ///   - {{ custom_parameters.plain | downcase }}: current {{ custom_parameters.plain | downcase }} model
    init({{ custom_parameters.plain | downcase }}: {{ custom_parameters.plain }}PlainObject) {
        self.{{ custom_parameters.plain | downcase }} = {{ custom_parameters.plain | downcase }}
    }
}
