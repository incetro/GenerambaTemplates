{% include 'header' %}

import Foundation

// MARK: - {{ custom_parameters.cell }}CellViewModelProtocol

protocol {{ custom_parameters.cell }}CellViewModelProtocol {
    
    /// Parent {{ plain_uncap }} object instance
    var {{ plain_uncap }}: {{ custom_parameters.plain }}PlainObject { get }
}
