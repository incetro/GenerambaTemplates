{% include 'header' %}

import Foundation

// MARK: - {{ custom_parameters.cell }}CellViewModel

struct {{ custom_parameters.cell }}CellViewModel: {{ custom_parameters.cell }}CellViewModelProtocol {
    
    // MARK: - Properties

    let {{ plain_uncap }}: {{ custom_parameters.plain }}PlainObject

    // MARK: - Initializers

    /// Default initializer
    /// - Parameters:
    ///   - {{ plain_uncap }}: current {{ plain_uncap }} model
    init({{ plain_uncap }}: {{ custom_parameters.plain }}PlainObject) {
        self.{{ plain_uncap }} = {{ plain_uncap }}
    }
}
