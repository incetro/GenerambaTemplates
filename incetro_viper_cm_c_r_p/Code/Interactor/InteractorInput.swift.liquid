{% include 'header' %}

import Foundation

// MARK: - {{ prefix }}{{ module_info.name }}InteractorInput

protocol {{ prefix }}{{ module_info.name }}InteractorInput {
    
    /// Obtain {{ entities_uncap }} list
    /// (pagination will be reset)
    func obtain{{ custom_parameters.entities }}()

    /// Obtain next {{ entities_uncap }} batch
    /// (pagination will be saved)
    func obtainNextPage()
}