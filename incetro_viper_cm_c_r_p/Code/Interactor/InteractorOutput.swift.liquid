{% include 'header' %}

// MARK: - {{ prefix }}{{ module_info.name }}InteractorOutput

protocol {{ prefix }}{{ module_info.name }}InteractorOutput: InteractorOutput {
    
    /// {{ custom_parameters.entities }} by the given request have been successfully obtained
    /// - Parameters:
    ///   - {{ entities_uncap }}: result {{ entities_uncap }}
    func obtain{{ custom_parameters.entities }}Success({{ entities_uncap }}: [{{ custom_parameters.plain }}PlainObject])

    /// There are no more objects left to obtain
    func pagintaionHasBeenCompleted()
}