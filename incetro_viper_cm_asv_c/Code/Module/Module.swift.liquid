{% include 'header' %}

import UIKit

// MARK: - {{ prefix }}{{ module_info.name }}Module

final class {{ prefix }}{{ module_info.name }}Module: AdvancedModule {

    // MARK: - Aliases

    typealias Input = {{ prefix }}{{ module_info.name }}ModuleInput
    typealias View = {{ prefix }}{{ module_info.name }}ViewController
    typealias Data = {{ custom_parameters.plain }}PlainObject

    // MARK: - Module

    static func instantiate(withData data: Data) -> {{ prefix }}{{ module_info.name }}ViewController {
		return {{ prefix }}{{ module_info.name }}ModuleAssembly().obtainViewController(with{{ custom_parameters.plain }}: data)
    }
}