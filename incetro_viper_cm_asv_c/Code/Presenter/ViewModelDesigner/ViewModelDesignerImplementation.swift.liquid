{% include 'header' %}

{% assign replacement = custom_parameters.plain }
{% assign plain_tail_count = replacement | size | minus: 1 %}
{% assign plain_first_letter = replacement | slice: 0 | downcase %}
{% assign plain_without_first_letter = replacement | slice: 1, plain_tail_count %}
{% assign plain_uncap = plain_first_letter | append: plain_without_first_letter %}
{% comment %}
{% It's necessary to use `swiftlint autocorrect` after generating this module because there will be empty lines %}
{% endcomment %}

import Foundation

// MARK: - {{ prefix }}{{ module_info.name }}ViewModelDesignerImplementation

final class {{ prefix }}{{ module_info.name }}ViewModelDesignerImplementation {

}

// MARK: - {{ prefix }}{{ module_info.name }}ViewModelDesigner

extension {{ prefix }}{{ module_info.name }}ViewModelDesignerImplementation: {{ prefix }}{{ module_info.name }}ViewModelDesigner {
	
	func viewModel(from {{ plain_uncap }}: {{ custom_parameters.plain }}PlainObject) -> {{ prefix }}{{ module_info.name }}ViewModelProtocol {
        {{ prefix }}{{ module_info.name }}ViewModel({{ plain_uncap }}: {{ plain_uncap }})
    }
}
